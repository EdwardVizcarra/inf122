import React, {useState} from 'react';

function Nota({nota, onDelte, onEdit}){
    const [editando, setEditando]= useState(false);
    const [nuevotexto, setNuevoTexto]= useState (nota.texto);
        return (
            <div>
                     
                
            </div>
        );
}
export default Nota;